<p id="notice"><%= notice %></p>

<turbo-frame id="main">
  <p>
    <strong>ID:</strong>
    <%= @order.id %>
  </p>
  <p>
    <strong>Status:</strong>
    <%= @order.status %>
  </p>
</turbo-frame>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
